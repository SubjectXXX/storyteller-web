/// <reference types="vite/client" />

// Minimal ambient declarations for the Storyteller SPA. vite/client (loaded
// via the triple-slash reference above) gives us `import.meta.env` typings;
// happy-dom supplies the rest of the browser globals in tests.
